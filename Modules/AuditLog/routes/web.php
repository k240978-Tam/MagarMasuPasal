<?php

// Audit log viewer UI ships in a later phase — see
// docs/architecture/08-roadmap.md Phase 5. This module currently provides
// the append-only audit_logs schema, the Auditable listener, and the
// AuditLogger service every other module writes through.
